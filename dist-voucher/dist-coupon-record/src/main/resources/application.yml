server:
  port: 10000

spring:
  application:
    name: dist-coupon-record
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.56.10:3306/dist-coupon-record?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

  cloud:
    # 配置优先使用本机ip网段，而不是虚拟机ip
    inetutils:
      preferred-networks: 192.168.0
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848


mybatis-plus:
  global-config:
    banner: false
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/**/*.xml